/**
 * GLOBAL RULES
 * Reset CSS...............Overwrite default browser settings
 * Variables...............To be reused throughout the document
 * Global rules............What applies in all document
 
 * FONTS
 * Font-size...............With class gathering
 * Font-weight.............With class gathering
 * Text-transform..........With class gathering
 *
 * COLORS
 *
 * IMAGES
 *
 * INTERACTIONS
 *
 * BACKGROUNDS
 *
 * PROGRESS BARS
 *
 * FORM
 */
 
/*-----------------------------*\
RESET CSS
\*-----------------------------*/

body {
	box-sizing: border-box;
	height: 100%;
	font-size: 1.5rem;
	vertical-align: baseline;
	background-color: #fff;
}

body, h1, h2, h3, article, figure, figcaption, blockquote, form, label, div, span, img, ul, li, section, header, main, footer, p {
	margin: 0;
	padding: 0;
}

a:link, a:visited, a:hover, a:active {
	text-decoration: none;
	color: var(--black);
}

ul {
	list-style: none;
}

/*-----------------------------*\
VARIABLES
\*-----------------------------*/

:root {
	--main-text-color: #686868;
	--primary-color: #fe7e17;
	--white: #fff;
	--black: #323232;
}

/*-----------------------------*\
GLOBAL RULES
\*-----------------------------*/

html {
	scroll-behavior: smooth;
	font-size: 62.5%;
}

section {
	max-width: 100%;
	margin: 1.5rem;
}

h1 {
	text-align: center;
}

li {
	display: inline-block;
}

.social-icons__item:link, 
.social-icons__item:visited {
	color: #848484;
	font-size: 1.4rem;
}

.social-icons__item:hover {
	color: var(--primary-color);
}

.section--bg-images {
	background-size: 100% 100%;
	background-repeat: no-repeat;
	background-attachment: fixed;
}

hr {
	margin: 1rem auto;
	width: 40%;
	background-color: var(--primary-color);
	border: none;
	height: .2rem;
}

.card__date .fas {
	border: 1px solid #ccc;
}

/*-----------------------------*\
FONTS
\*-----------------------------*/

body {
	font-family: 'Open Sans', Helvetica, Arial, sans-serif;
}

/*-----------------------------*\
FONT-SIZE
\*-----------------------------*/

h1 {
	font-size: 3.6rem;
}

.main-nav__label {
	font-size: 2.8rem;
}

.card__title {
	font-size: 2.6rem;
}

.blog-quote {
	font-size: 2.4rem;
}

.intro, .twitter-testimonial__text, 
.testimonials__text, 
.contact__title {
	font-size: 1.8rem;
}

.card__title--little, 
.card__subtitle, 
.quote__author, 
.album-menu__item, 
.fun-facts__facts, 
.services__container__services-name, 
.blog-quote__author, 
.twitter-testimonial__date, 
.twitter-icons__item, 
.testimonials__author, 
.contact-list, 
.form__area, 
.copyright {
	font-size: 1.5rem
}

.quote__text {
	font-size: 3rem;
}

.fun-facts__number, 
.services__container__services-icon {
	font-size: 5.6rem;
}

.card__date, 
.card__footer__read-more, 
.form__button, 
.progress__value {
	font-size: 1.2rem;
}

/*-----------------------------*\
FONT-WEIGHT
\*-----------------------------*/

body {
	font-weight: 300;
}

.main-nav__link--bold, 
.fun-facts__number, 
.quote__author, 
.services__container__services-name, 
.card__footer__read-more, 
.blog-quote__author, 
.testimonials__author, 
.form__button, 
.progress__type {
	font-weight: 700;
}

.card__title {
	font-weight: 800;
}

/*-----------------------------*\
TEXT-TRANSFORM
\*-----------------------------*/

h1, 
.card__title--uppercase, 
.main-nav__link--second, 
.main-nav__link, 
.fun-facts__facts, 
.services__container__services-name, 
.card__date, 
.card__footer__read-more, 
.blog-quote__author, 
.form__button {
	text-transform: uppercase;
}

/*-----------------------------*\
COLORS
\*-----------------------------*/

body {
	color: var(--main-text-color);
}

h1, 
.intro, 
.services__container__services-name, {
	color: #444;
}

.card__footer__read-more:link, 
.twitter-testimonial__link:link, 
.testimonials__author-name, 
.testimonials__link:link, 
.contact-list__link:link, 
.contact-list__link:visited {
	color: var(--primary-color);
}

.main-nav__label, 
.card__title, 
.card__subtitle, 
.fun-facts__number, 
.services__container__services-icon, 
.testimonials__author, 
.contact-list .fas, 
.progress__type, 
.contact__info .fas {
	color: var(--black);
}

.quote__text, 
.blog-quote, 
.twitter-testimonial h1, 
.twitter-icons__item:link, 
.contact, 
.contact__title, 
.contact h1, 
.contact-list, 
.form__button, 
.progress__value {
	color: var(--white);
}

.card__date, 
.blog-quote__author {
	color: #a3a2a2;
}

.twitter-testimonial__date, 
.form__area {
	color: #fafafa;
}

.twitter-testimonial__text {
	color: #efefef;
}

.copyright, 
.social-icons__item--border {
	color: #868686;
}

/*-----------------------------*\
IMAGES
\*-----------------------------*/

img {
	max-width: 100%;
	height: auto;
}

.card__figure {
	max-width: 100%;
	margin: 0 auto;
	position: relative;
}

.card__image {
	filter: grayscale(100%);
}

.image__link-icon:link {
	position: absolute;
	top: 50%;
	left: 50%;
	transform: translate(-50%, -50%);
	color: transparent;
}

/*-----------------------------*\
INTERACTIONS
\*-----------------------------*/

.card__footer__read-more:hover, 
.testimonials__link:hover {
	color: #444;
}

.main-nav__link--second:link, 
.main-nav__link--second:visited {
	color: var(--black);
}

.main-nav__link--second:hover, 
.main-nav__link--second:active, 
.card__title--orange:hover, 
.twitter-icons__item:hover {
	color: var(--primary-color);
}

.services__container:hover .services__container__services-icon, 
.services__container:hover .services__container__services-name, 
.twitter-testimonial__link:hover, 
.contact-list__link:hover {
	color: var(--white);
}

.form__button:hover {
	background-color: #222;
}

.main-nav__link--underline:hover, 
.main-nav__link--underline:active {
	text-decoration: underline;
}

.card__figure:hover .card__image {
	filter: grayscale(0%) brightness(65%);
}

.card__figure:hover .image__link-icon {
	color: var(--white);
	border: 1px solid var(--white);
	padding: .2rem .5rem;
}

.album-menu__item:hover {
	border: 1px solid var(--primary-color);
}

.services__container:hover {
	background-color: var(--primary-color);
}

.services__container:hover .services__container__services-icon {
	color: var(--white);
}

.services__container:hover .services__container__services-name {
	color: var(--white);
}

.main-nav__input {
	visibility: hidden;
}

.main-nav__list--hidden {
	display: none;
}

nav input[type=checkbox]:checked + .main-nav__list {
	display: block;
	position: absolute;
	background-color: var(--white);
}

/*-----------------------------*\
BACKGROUNDS
\*-----------------------------*/

.section--bg-images {
	background-size: 100% 100%;
	background-repeat: no-repeat;
	background-attachment: fixed;
}

.main-nav, .contact__info .fas {
	background-color: var(--white);
}

.quote {
	background-image: url("images/parallax-quote.jpg")
}

.services {
	background-image: url("images/parallax-services-transparent.png");
}

.blog-quote {
	background-color: var(--black);
}

.twitter-testimonial {
	background-image: url("images/parallax-twitter-feed-dark.png");
}

.partners {
	background-color: var(--primary-color);
}

.contact {
	background-image: url("images/parallax-contact-dark.png");
}

.contact-list .fas {
	background-color: var(--white);
}

/*-----------------------------*\
PROGRESS BAR
\*-----------------------------*/

.progress__container {
	width: 100%;
}

.progress__bar {
	width: 100%;
	height: 1rem;
	margin-bottom: 2rem;
}

.progress__value {
	background-color: var(--black);
	color: var(--white);
	position: relative;
	border-radius: .3rem;
	padding: .2rem .4rem;
}

.progress__value--html {
	left: 11.9rem;
}

.progress__value--illustrator {
	left: 3.5rem;
}

.progress__value--photoshop {
	left: 17.5rem;
}

.arrow {
	display: block;
	width: 0;
   height: 0;
   position: absolute;
	left: .7rem;
   border-right: .8rem solid transparent;
   border-left: .8rem solid transparent;
   border-top: 1rem solid var(--black);
}

.progress__label {
	margin-bottom: 1rem;
}

progress[value] {
	-webkit-appearance: none;
	-moz-appearance: none;
	appearance: none;
	border: none;
}

progress[value]::-webkit-progress-bar, .progress__bar[value]::-moz-progress-bar {
    background-color: var(--primary-color);    
}

/*-----------------------------*\
FORM
\*-----------------------------*/

.form__area {
	width: 85%;	
	background-color: transparent;
	padding: 1rem 1.5rem;
	margin-bottom: 2rem;
	margin-left: .5rem;
	border: .1rem solid var(--white);
}

.form__button {
	width: 7rem;
	height: 3.4rem;
	padding: 1rem 2rem;
	background-color: var(--black);
	margin-bottom: 2rem;
}















	
	
	
	
	
	
	
	
	
	
	
	
	
	
	
	







